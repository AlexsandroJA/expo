unzip precificador.zip
cd precificador

# Instalar dependências
npm install

# Instalar CLI do EAS\NNnpm install -g eas-cli

# Fazer login no Expo (use seu e-mail/senha ou GitHub)
eas login

# Inicializar a configuração do EAS
eas build:configure

# Iniciar build do APK
eas build --platform android --profile preview

// App.js
import React, { useState } from 'react';
import { View, Text, TextInput, StyleSheet, Button, ScrollView } from 'react-native';

export default function App() {
  const [gramasResina, setGramasResina] = useState('');
  const [valorGrama, setValorGrama] = useState('0.35');
  const [custoInsumos, setCustoInsumos] = useState('');
  const [margemLucro, setMargemLucro] = useState('250');
  const [resultado, setResultado] = useState(null);

  const calcularPreco = () => {
    const gramas = parseFloat(gramasResina);
    const valor = parseFloat(valorGrama);
    const insumos = parseFloat(custoInsumos);
    const margem = parseFloat(margemLucro);

    if (isNaN(gramas) || isNaN(valor) || isNaN(insumos) || isNaN(margem)) {
      alert('Preencha todos os campos corretamente.');
      return;
    }

    const custoTotal = (gramas * valor) + insumos;
    const precoVenda = custoTotal * (1 + margem / 100);

    setResultado({
      custo: custoTotal.toFixed(2),
      preco: precoVenda.toFixed(2),
    });
  };

  return (
    <ScrollView contentContainerStyle={styles.container}>
      <Text style={styles.title}>Precificador de Peças</Text>

      <Text>Gramas de resina utilizadas:</Text>
      <TextInput
        style={styles.input}
        keyboardType="numeric"
        value={gramasResina}
        onChangeText={setGramasResina}
        placeholder="Ex: 50"
      />

      <Text>Valor da grama da resina (R$):</Text>
      <TextInput
        style={styles.input}
        keyboardType="numeric"
        value={valorGrama}
        onChangeText={setValorGrama}
      />

      <Text>Custo com insumos (R$):</Text>
      <TextInput
        style={styles.input}
        keyboardType="numeric"
        value={custoInsumos}
        onChangeText={setCustoInsumos}
      />

      <Text>Margem de lucro (%):</Text>
      <TextInput
        style={styles.input}
        keyboardType="numeric"
        value={margemLucro}
        onChangeText={setMargemLucro}
      />

      <Button title="Calcular Preço" onPress={calcularPreco} />

      {resultado && (
        <View style={styles.resultado}>
          <Text>Custo Total: R$ {resultado.custo}</Text>
          <Text>Preço de Venda: R$ {resultado.preco}</Text>
        </View>
      )}
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {
    padding: 20,
    paddingTop: 60,
  },
  title: {
    fontSize: 24,
    marginBottom: 20,
    fontWeight: 'bold',
  },
  input: {
    borderWidth: 1,
    borderColor: '#ccc',
    padding: 10,
    marginBottom: 15,
    borderRadius: 5,
  },
  resultado: {
    marginTop: 30,
    padding: 15,
    backgroundColor: '#e0ffe0',
    borderRadius: 10,
  },
});

